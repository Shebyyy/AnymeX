name: Upload AnimatedBottomBar ZIP

on:
  workflow_dispatch

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload AnimatedBottomBar ZIP
        uses: actions/upload-artifact@v4
        with:
          name: animated-bottom-bar
          path: AnimatedBottomBar.zip
